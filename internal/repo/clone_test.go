package repo

import (
	"testing"
)

// TestClone tests the Clone function
func TestClone(t *testing.T) {
	t.Skip("Skipping test that requires network access - use 'make test-integration' to run")
}

// TestCloneToExistingDirectory tests cloning to a directory that already exists
func TestCloneToExistingDirectory(t *testing.T) {
	t.Skip("Skipping test that requires network access - use 'make test-integration' to run")
}
